apiVersion: controller.kubequant.com/v1
kind: KubeQuantPodResource
metadata:
  labels:
    app.kubernetes.io/name: kubequant
    app.kubernetes.io/managed-by: kustomize
  name: kubequantpodresource-sample
spec:
  # TODO(user): Add fields here
  labels: "app=nginx"
